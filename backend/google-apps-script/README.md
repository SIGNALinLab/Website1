# Google Apps Script backend

Run `initializeSignalSheets()` from the target Sheet-bound Apps Script project, deploy as a Web app, then configure `appsScriptEndpoint`. The backend accepts only application/contact schemas and escapes spreadsheet-formula prefixes.
