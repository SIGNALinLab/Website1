document.addEventListener('signal:ready',async()=>{const page=document.body.dataset.page;if(page==='people'){const d=await(await fetch('assets/data/people.json')).json();document.querySelector('#people-list').innerHTML=d.records.length?d.records.map(p=>`<article class="card"><h3>${p.name}</h3><p>${p.role}</p></article>`).join(''):'<div class="notice info">People/Staff source content has not been supplied in this design pass. The page shell is ready for the owner-verified roster.</div>'}if(page==='equipment'){const d=await(await fetch('assets/data/equipment.json')).json();document.querySelector('#equipment-list').innerHTML=d.records.map(x=>`<article class="card"><span class="badge">${x.status}</span><h3>${x.name}</h3><p><strong>Category:</strong> ${x.category}<br><strong>Model:</strong> ${x.model}</p></article>`).join('')}});