const verifyEsc=s=>String(s??'').replace(/[&<>\'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt',"'":'&#39;','"':'&quot;'}[c]));document.addEventListener('signal:ready',async()=>{if(document.body.dataset.page!=='verify')return;const data=await(await fetch('assets/data/recommendations.json')).json(),input=document.querySelector('#ref-id');const records=Array.isArray(data.records)?data.records:[];function go(id){const r=records.find(x=>String(x.id||'').toUpperCase()===String(id||'').toUpperCase()),o=document.querySelector('#verify-result');o.innerHTML=r?`<div class="card verification"><h2>${verifyEsc(String(r.status||'').toUpperCase())}</h2><p><strong>Reference ID:</strong> ${verifyEsc(r.id)}<br><strong>Applicant:</strong> ${verifyEsc(r.applicantName)}<br><strong>Issue date:</strong> ${verifyEsc(r.issueDate)}<br><strong>Document type:</strong> ${verifyEsc(r.documentType)}<br><strong>Issuer:</strong> ${verifyEsc(r.issuer)}</p></div>`:'<div class="card verification"><h2>Not found</h2><p>No public validation record matches this reference ID.</p></div>'}const q=new URLSearchParams(location.search).get('id')||'';input.value=q;if(q)go(q);document.querySelector('#verify-form').addEventListener('submit',e=>{e.preventDefault();go(input.value.trim())})});