document.addEventListener('signal:ready',()=>{const c=SIGNAL.config||{};const app=document.querySelector('#application-mount'),contact=document.querySelector('#contact-form-mount');if(app){if(c.applyMode==='external'&&c.googleFormUrl){app.innerHTML=`<div class="form-fallback notice"><strong>Application form</strong><p>Applications are currently collected through the configured Google Form.</p><a class="btn btn-primary" href="${c.googleFormUrl}" target="_blank" rel="noopener">Open application form ↗</a></div>`}else if(!c.appsScriptEndpoint){app.innerHTML='<div class="form-fallback notice">Native submission is not configured. Please contact the lab by email.</div>'}}if(contact&&!c.appsScriptEndpoint){contact.innerHTML=`<div class="form-fallback notice"><strong>Email enquiries</strong><p>The native contact endpoint is not configured. Please email <a href="mailto:${c.email}">${c.email}</a>.</p></div>`}});