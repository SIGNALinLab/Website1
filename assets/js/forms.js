
function safeText(s){return String(s??'').replace(/^[=+\-@]/,m=>"'"+m)}
async function submitSignalForm(form,kind){const status=form.querySelector('.form-status');const btn=form.querySelector('button[type="submit"]');const c=SIGNAL.config||{};if(!c.appsScriptEndpoint){status.textContent='Form submission is not configured yet. Please use the listed email address.';status.className='form-status notice info';return}const fd=new FormData(form),payload={formType:kind};for(const [k,v] of fd.entries())payload[k]=safeText(v);btn.disabled=true;status.textContent='Submitting…';try{const r=await fetch(c.appsScriptEndpoint,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify(payload)});const out=await r.json();if(!out.ok)throw new Error(out.error||'Submission failed');form.reset();status.textContent='Submission received. This is not an offer of admission, funding, employment, or supervision.';status.className='form-status notice';}catch(e){status.textContent='Submission could not be completed. Please contact the lab by email.';status.className='form-status notice danger';}finally{btn.disabled=false}}
document.addEventListener('signal:ready',()=>{document.querySelectorAll('form[data-signal-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();submitSignalForm(f,f.dataset.signalForm)}))});
