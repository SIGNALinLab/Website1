let PROJECTS=null;const esc=s=>String(s??'').replace(/[&<>\'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));const safeProjectUrl=v=>{try{const u=new URL(String(v||''),location.href);return /^https?:$/.test(u.protocol)?u.href:''}catch{return''}};
const FACT={funder:'<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M3 21h18M5 21V9l7-4 7 4v12M9 21v-6h6v6"/></svg>',amount:'<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><ellipse cx="9" cy="7" rx="5" ry="2.5"/><path d="M4 7v8c0 1.4 2.2 2.5 5 2.5"/><ellipse cx="16" cy="14" rx="4" ry="2"/></svg>',duration:'<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="4" y="5" width="16" height="15" rx="2"/><path d="M8 3v4M16 3v4M4 10h16M12 13v3l2 1"/></svg>'};
function typeChip(type){return `<span class="project-type-chip ${type}">${type==='gpo'?'GPO':'SDP'}</span>`}function fact(label,val,type){return `<div class="fact"><div class="fact-label"><span class="fact-icon">${FACT[type]}</span><span>${label}</span></div><div class="fact-value">${esc(val)}</div></div>`}function links(arr=[]){return arr.map(l=>{const u=safeProjectUrl(l.url);if(!u)return'';let icon=l.type==='youtube'?'▶':l.type==='github'?'⌘':'↗';return `<a class="resource-link" href="${esc(u)}" target="_blank" rel="noopener"><span class="resource-icon" aria-hidden="true">${icon}</span>${esc(l.label)}</a>`}).join('')}
function previewCard(p,type){const m=p.media,src=m.preview||m.src;return `<article class="card project-preview"><div class="project-preview-media"><img src="${esc(src)}" width="${m.previewWidth||m.width}" height="${m.previewHeight||m.height}" loading="lazy" alt="${esc(m.alt)}"></div><div class="project-preview-body"><div class="project-preview-meta"><span class="project-preview-type project-type-${type}">${type==='gpo'?'GPO':'SDP'}</span><span>${esc(p.duration)}</span></div><h3>${esc(p.title)}</h3></div></article>`}
function projectRecord(p,type){const m=p.media;return `<article class="project-record"><div><div class="project-kicker-row">${typeChip(type)}<span class="project-status-chip">${esc(p.status)}</span></div><h2>${esc(p.title)}</h2><div class="project-facts">${fact('Funder / support',p.funder,'funder')}${fact('Amount',p.amount,'amount')}${fact('Duration',p.duration,'duration')}</div><p>${esc(p.description)}</p><div class="project-team"><h3>Project team</h3><ul>${p.team.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div class="project-focus"><h3>Technical focus</h3><div class="chip-row">${p.technologies.map(x=>`<span class="chip">${esc(x)}</span>`).join('')}</div></div>${p.award?`<div class="award-note"><strong>Project recognition</strong><p>${esc(p.award.text)}</p></div><figure class="award-media"><img src="${esc(p.award.image)}" width="${p.award.width}" height="${p.award.height}" loading="lazy" alt="${esc(p.award.alt)}"></figure>`:''}<div class="project-resources"><h3>Project resources</h3><div class="resource-links">${links(p.links)}</div></div></div><div class="project-media"><span class="project-demo-label">▶ Demo</span><img src="${esc(m.src)}" width="${m.width}" height="${m.height}" loading="lazy" alt="${esc(m.alt)}"></div></article>`}
document.addEventListener('signal:ready',async()=>{if(!['home','projects','gpo','sdps'].includes(document.body.dataset.page))return;PROJECTS=await(await fetch('assets/data/projects.json')).json();const g=PROJECTS.groups.gpo.records,s=PROJECTS.groups.sdp.records;if(document.body.dataset.page==='home'){document.querySelector('#home-projects').innerHTML=[g[0],s[0],s[1]].map((p,i)=>previewCard(p,i? 'sdp':'gpo')).join('')}if(document.body.dataset.page==='projects'){document.querySelector('#featured-projects').innerHTML=[g[0],g[2],s[0]].map((p,i)=>previewCard(p,i===2?'sdp':'gpo')).join('')}if(document.body.dataset.page==='gpo'){document.querySelector('#project-list').innerHTML=g.map(p=>projectRecord(p,'gpo')).join('')}if(document.body.dataset.page==='sdps'){document.querySelector('#project-list').innerHTML=s.map(p=>projectRecord(p,'sdp')).join('')}});