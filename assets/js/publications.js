let P=[],active='all';const E=s=>String(s??'').replace(/[&<>\'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));const safePublicationUrl=v=>{try{const u=new URL(String(v||''),location.href);return /^https?:$/.test(u.protocol)?u.href:''}catch{return''}};const labels={medicine:'SIGNAL in Medicine',security:'SIGNAL in Defense & Security',education:'SIGNAL in Education',energy:'SIGNAL in Energy & Sustainability',ai:'SIGNAL in AI & Optimization'};function authors(s){return E(s).replace(/Abdulaziz(?:\s+A\.)?\s+Alorf/gi,m=>`<strong>${m}</strong>`)}function render(){const q=(document.querySelector('#pub-search').value||'').toLowerCase();const f=P.filter(p=>(active==='all'||p.category===active)&&(!q||[p.title,p.authors,p.venue,p.doi,p.year].join(' ').toLowerCase().includes(q)));document.querySelector('#pub-count').textContent=`${f.length} publication${f.length===1?'':'s'} shown`;document.querySelector('#pub-list').innerHTML=f.map(p=>`<article class="pub-card"><span class="badge">${E(labels[p.category])}</span><h3>${E(p.title)}</h3><div class="pub-authors">${authors(p.authors)}</div><div class="pub-meta"><span>${E(p.year)}</span><span>${E(p.type)}</span><span>${E(p.venue)}</span>${p.doi?`<span>DOI: ${E(p.doi)}</span>`:''}</div>${safePublicationUrl(p.url)?`<a class="external" href="${E(safePublicationUrl(p.url))}" target="_blank" rel="noopener">Open publication source ↗</a>`:''}</article>`).join('')||'<p class="notice">No records match the current filters.</p>'}document.addEventListener('signal:ready',async()=>{if(document.body.dataset.page!=='publications')return;const d=await(await fetch('assets/data/publications.json')).json();P=d.records.sort((a,b)=>b.year-a.year);document.querySelector('#audit-date').textContent=new Date(d.lastAudited+'T00:00:00').toLocaleDateString('en-US',{year:'numeric',month:'long',day:'numeric'});document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{active=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));render()}));document.querySelector('#pub-search').addEventListener('input',render);render()});